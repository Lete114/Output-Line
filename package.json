{
  "name": "output-line",
  "version": "0.0.1",
  "description": "执行node程序时，控制台中输出console行号 | When executing a node program, the console outputs the console line number",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "lint": "npx eslint . && npx prettier --check .",
    "lint:fix": "npx eslint --fix . && npx prettier --check --write ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Lete114/Output-Line"
  },
  "keywords": [
    "console",
    "log",
    "node",
    "line",
    "line",
    "number"
  ],
  "author": "Lete乐特",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.9.0",
    "prettier": "^2.5.1"
  }
}
